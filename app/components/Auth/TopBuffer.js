import React from 'react';
import { View } from 'react-native';
import styles from './styles';

const TopBuffer = (props) => {
  return (
    <View style={styles.TopBuffer} >
    </View>
  )
}

export default TopBuffer;
